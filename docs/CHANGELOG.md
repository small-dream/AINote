# 更新日志

## v0.12.2 — 2026-09-02

### 体验优化与缺陷修复

- 编辑器大纲改为悬浮入口，移除多余标题和左侧折叠箭头，增加平滑显示/隐藏动画。
- 所见即所得编辑区增加阅读留白，滚动条保持贴合编辑区域右侧。
- 修复富文本大纲在空标题、重复标题和布局更新时定位不准确或无响应的问题。

## v0.12.1 — 2026-09-02

### 缺陷修复

- 修复软渲染行内标记（加粗/斜体/删除线/行内代码）在 macOS（WebKit）下光标停靠于隐藏标记时无法定位（坐标为空），导致光标消失/错位、输入异常、视觉上“多出空格/抖动”的问题：隐藏样式由 `font-size: 0` 改为 `1px + 透明色`，各内核均可正确定位光标且视觉仍不可见。

## v0.12.0 — 2026-09-02

### AI 能力（新增）

- 设置 → AI：支持 OpenAI 兼容 API 与 Ollama 本地端点，可配置 Base URL / 模型 / API Key / 启用开关；API Key 以 AES-256-GCM 加密存储，前端拿不到明文；未配置时编辑器与问答入口显示引导直达设置。
- 编辑器 AI 写作：选中文本支持润色 / 翻译 / 缩写 / 扩写，光标处支持续写；结果先预览确认再落笔，Markdown（CodeMirror）与富文本（TipTap）编辑器均可用。
- AI 问答面板：可基于「仅当前笔记」或「当前笔记 + 全库关键词检索」提问，回答以 Markdown 渲染并可一键插入笔记。
- 流式输出（打字机效果）：写作与问答均按 SSE 增量实时展示，降低等待感知。
- 一键生成笔记摘要：写入 frontmatter `summary`（可再次覆盖），正文保持不变。
- 标题 / 大纲建议：标题建议产出候选列表、单选即替换首行标题；大纲建议产出层级大纲、可一键插入文末。
- 架构：新增 `commands/ai`、`services/{ai_service,ai_store,secure_store}`、`repositories/llm.rs`、`domain/ai.rs` 与 `AI_6xxx` 错误域；前端新增 `features/ai`（写作 / 问答 / 建议）与 `settings/AiSettings`，错误与隐私边界沿用既有防腐化规范。

## v0.11.1 — 2026-09-02

### 缺陷修复

- 修复软渲染加粗/斜体/删除线/行内代码在光标进入时重新显示原始标记（`**` 等冒出、段落宽度抖动）的问题：改为零宽隐藏，编辑时标记始终不显示、不占宽度。

## v0.11.0 — 2026-09-02

### 体验优化

- 软渲染编辑加粗/斜体/删除线/行内代码时不再淡显原始标记，始终保持渲染效果（消除整段文字宽度突变导致的抖动）；标题/链接/图片/表格等结构元素仍会在编辑时淡显原始标记以便就地修改。
- 所见即所得（软渲染）模式隐藏编辑器行号与活动行标记，仅保留内容排版；源码/分栏模式仍显示行号。
- 预览模式正文限行宽并水平居中（大屏阅读不再贴左、留白对称）；Markdown 预览与富文本编辑均可开关左侧大纲目录，点击标题跳转定位；富文本编辑内容同样限宽居中。

## v0.10.2 — 2026-09-02

### 缺陷修复

- 修复软渲染编辑器点击行内部分区域时光标总回到行首的问题。
- 修复手动滚动后点击编辑可能跳到其他行的问题，改为按实际命中的 DOM 行计算源码位置。
- 增加软渲染点击与 selection 更新诊断日志，便于定位浏览器坐标映射问题。

## v0.10.1 — 2026-09-01

### 缺陷修复

- 分栏视图下编辑侧固定为源码编辑（恢复原始 Markdown 输入），软渲染仅用于单栏编辑视图；分栏模式下隐藏软渲染切换按钮。

## v0.10.0 — 2026-09-01

### Markdown 软渲染（WYSIWYG）

- Markdown 编辑默认启用 CodeMirror 软渲染（Typora 式所见即所得）：标题（ATX 与 Setext）、加粗/斜体/删除线、行内代码、围栏代码块（语法高亮）、引用/Callout、列表与任务勾选、链接/双链、图片、表格、分割线、数学公式（KaTeX）与 Mermaid 图表直接在编辑区渲染。
- 光标或选区命中的元素淡显原始标记以便就地编辑；点击已渲染的代码块/表格/图片等 widget 会自动进入源码编辑，任务项支持点击或空格键切换勾选。
- `Ctrl+/`（macOS 为 `⌘/`）或工具栏按钮在「软渲染 / 源码」间切换，偏好按笔记持久化，默认软渲染；源码 100% 原样保存与提交，Git 保真、全文搜索与 wiki 索引不受影响。
- 新增外部链接点击打开能力（仅允许 http/https 协议），链接/双链可在编辑区直接点击。
- 性能优化：保护区改为有序区间索引 + 二分查询，5000 行密集双链文档单次重算约 5ms，无明显卡顿。

## v0.9.0 — 2026-09-01

### 应用图标

- 重新设计应用图标：采用简约扁平风格，品牌蓝纯色圆角方底 + 白色几何 `N` 字标，移除原有的 Git 分支元素。
- 同步更新 Windows、macOS、iOS、Android 各平台全套图标。

## v0.8.0 — 2026-09-01

### 富文本代码块语言选择

- 代码块头部内嵌语言下拉（参考 Typora/Notion），点击即可从 37 种常见语言中选择，也可切回「自动检测」。
- 语言切换即时生效并随笔记保存，Markdown 互转时保留所选语言，兼容 `ts`、`js` 等常见别名。

## v0.7.0 — 2026-09-01

### 富文本代码块语法高亮

- 富文本编辑器代码块接入 Lowlight（highlight.js）语法高亮，支持 37 种常见语言。
- 指定语言（如 ` ```ts `）按语言着色，未指定语言时自动检测。
- 代码块顶部显示语言徽标，配色与 Markdown 预览共用同一套语义色并随主题切换。

## v0.6.4 — 2026-09-01

### 缺陷修复

- 笔记列表和目录树隐藏 `.md` / `.ainote` 后缀。
- 恢复 Markdown 笔记的 `M` 图标，富文本笔记使用独立图标。
- 修复富文本笔记打开后内容丢失的问题。

## v0.6.3 — 2026-09-01

### 发布修复

- 补齐保存失败重试文案和 Rust 锁定版本，确保发布构建与本地版本一致。

## v0.6.2 — 2026-09-01

### 编辑体验优化

- 自动保存改为停止输入 3 秒后写入本地文件。
- 工作区空闲 15 分钟后批量生成 `note: auto commit`。
- 移除编辑器常驻保存按钮，保留 Cmd/Ctrl+S 快捷保存与失败重试。
- 笔记列表和详情隐藏 `.md` / `.ainote` 后缀；富文本笔记使用独立图标。

## v0.6.1 — 2026-09-01

### 富文本与 Markdown 互转

- 新增「一键转换笔记类型」：富文本工具栏「转换为 Markdown」、Markdown 顶部栏「转换为富文本」，转换后自动提交 Git 并打开新类型笔记。
- 转换走原子命令（`convert_note`）：写新扩展名文件后删除旧文件；内容由前端即时生成（富文本经 `tiptap-markdown`，Markdown 经 TipTap 解析）。
- 新增转换纯函数测试与 Rust 转换仓库测试。

## v0.6.0 — 2026-09-01

### 真富文本编辑（.ainote）

- 新增「真富文本」笔记类型：新建时可选 Markdown / 富文本，富文本以 TipTap JSON（`.ainote`）存储并走 Git 版本化。
- 富文本所见即所得编辑器：标题、加粗、斜体、删除线、行内代码、引用、无序/有序/任务列表、代码块、分割线。
- 图片导入到仓库 `assets/`，存仓库相对路径，跨设备可移植；3×3 表格插入、列宽拖拽与删除表格。
- `[[双链]]` 与 `#标签` 可视化 mark，渲染为可点击元素：双链跳转目标笔记，标签跳转侧边栏标签索引。
- 斜杠命令 `/`：输入弹出命令菜单，支持标题/列表/任务/引用/代码块/表格/分割线/行内格式，键盘上下选择、回车执行。
- 选中文本气泡菜单：块级与行级格式一键切换。
- Markdown 互转导出：富文本内容可复制为 Markdown / 粘贴 Markdown 导入（`tiptap-markdown`）。

### 工程质量

- 富文本索引走双管道：Rust 提取 TipTap JSON 纯文本，富文本笔记进入全文搜索、标签与双链索引。
- 新增斜杠命令过滤、标签/双链 mark 等纯函数测试，工作区标签跳转状态覆盖用例。

## v0.5.3 — 2026-09-01

### Markdown 编辑与预览

- 增加经典、纸张、夜航、森林四套编辑器与预览主题。
- 主题可从编辑器工具栏即时切换，并持久化到本地。
- 主题同时覆盖 CodeMirror、代码块、Callout、表格、链接和分栏预览。

## v0.5.2 — 2026-09-01

### 桌面窗口体验

- 修复隐藏标题栏模式下无法从顶部空白区域拖动窗口的问题。
- 左侧功能导航栏由 88px 收窄至 72px，在保留操作热区的同时增加内容空间。

## v0.5.1 — 2026-09-01

### Markdown 编辑与预览

- 修复编辑/预览分栏模式持续抖动的问题。
- 滚动位置持久化不再触发重复渲染，拖拽分栏比例时保持滚动状态稳定。

## v0.5.0 — 2026-09-01

### Markdown 编辑与预览

- 增加 `[[双链]]` 笔记目标补全和 `#标签` 自动补全。
- 预览中区分已解析与未解析双链，并为未解析目标提供明确状态提示。
- 反向链接展示来源标题、路径和引用上下文片段。
- 按仓库与笔记记忆编辑/分栏/预览模式、分栏比例和滚动位置。
- Cmd/Ctrl+K 命令面板支持切换视图模式和插入 Callout。

### 工程质量

- 双链索引提供行级上下文，前后端 DTO 保持类型一致。
- 新增编辑器偏好和 Callout 插入纯函数测试。
- 保持 Tauri updater 签名校验和 Markdown 原始 HTML 安全边界。
